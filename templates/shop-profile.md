# Shop Profile — <Shop Name>

Lives at `estimate-desk/shop-profile.md`, relative to the workspace root (NOT
inside the skill folder). Copy this, fill in what you know,
leave the rest blank. Blanks become questions later — they don't block the
first job.

```markdown
# Shop Profile — <Shop Name>

## Basics
Mode: retailer | wholesale middle man | both
Approver(s): <name> <email>
Outbound mailbox: <email>        CC always: <emails>
Signature block: <as it should appear>
Disclose agent as assistant: yes | no

## Autonomy
Trust stage: 1 | 2 | 3              # 1 = draft only. Default 1.
Price approval: always owner          # all estimates require owner approval before sending
Standing exceptions: <none | price-free questions | booking in windows>
Last changed: <date> by <who>

## Rate card (from invoices, dated <date>)
Metal:  14K $<x>/g · 18K $<x>/g · Plat $<x>/g · Silver $<x>/g
Stones: natural melee $<x>/ct · lab melee $<x>/ct
        center by size band: <0.5-1ct $<x>/ct · 1-2ct $<x>/ct · 2ct+ $<x>/ct>
        natural vs lab multiplier: <x>
Fees:   CAD $<x> · casting $<x> · setting $<x>/stone · finishing $<x>
        plating $<x> · engraving $<x>/char or $<x>/piece
Labor:  $<x>/hr bench          # REQUIRED. Do not leave blank.
Markup: <x>% or <x>x COGS      # trade markup if different: <x>

## Typical finished weights
<piece type>: <g range>
# e.g. 6mm men's band: 8-11g | solitaire head+shank: 4-5g | 18in 2mm chain: 3-4g

## Comparable jobs
<spec> -> cost $<x> -> sold $<x> (<date>)
# The middle man's whole engine. Add one line after every job.

## Defaults when unstated (assume, state in the brief, don't wait)
Karat: <x>          Metal color: <x>       Band width: <x>mm
Setting: <x>        Finish: <x>            Chain gauge: <x>
Stone origin: ASK ALWAYS -- never default lab or natural

## Scheduling
Booking mode: preset windows | ask-owner-each-time
Timezone: <IANA, e.g. America/Los_Angeles>
Calendar: <id / account>
Windows: Mon-Fri <10:00-17:00> · Sat <11:00-15:00> · Sun closed
Blackouts: <lunch 12:30-13:30> · <dates closed>
Durations: call <20m> · video <30m> · in-person consult <60m>
           try-on/sizing <20m> · pickup <15m> · trade call <15m>
Buffer after in-person: <15m>   Min notice: <2h>   Book out to: <30 days>
Location: <showroom address>    Video: <how links are created>
Must also attend: <bench / gemologist / partner>

## Terms
Lead time: <x> business days     Rush: +<x>% if feasible
Deposit: <x>%                    Tax: <handling>
Quote valid: <x> days            Rendering policy: <offer | on request | never>

## Systems
CRM/POS: <system>   Write access: yes | no
```

## Filling it in

**Required before the first quote:** mode, markup, approver, outbound mailbox.
Four fields, ninety seconds.

**Required before the first booking:** timezone, calendar, windows, durations.

**The two that decide accuracy:** `Labor` and `Typical finished weights`.
Everything else can be a market average. These two cannot — they're specific
to this shop, and they're where estimates go wrong.

**Re-confirm quarterly**, or whenever metal moves hard, hours change, or the
owner adjusts the trust stage.
